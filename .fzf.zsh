# Setup fzf
# ---------
if [[ ! "$PATH" == */home/erfan/.fzf/bin* ]]; then
  PATH="${PATH:+${PATH}:}/home/erfan/.fzf/bin"
fi

# Auto-completion
# ---------------
# source "$HOME/.fzf/shell/completion.zsh"

# Key bindings
# ------------
# source "$HOME/.fzf/shell/key-bindings.zsh"
